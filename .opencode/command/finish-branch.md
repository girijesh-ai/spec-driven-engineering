---
description: Land the branch after review-code returned READY — merge, PR, or rebase.
---

Load the `finish-branch` skill and follow it. $ARGUMENTS is optional landing preference (merge, PR, rebase).
