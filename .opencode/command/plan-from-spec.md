---
description: Turn a spec into an ordered implementation plan with per-step verification.
---

Load the `plan-from-spec` skill and follow it. $ARGUMENTS is the spec path or the confirmed bounded design.
