---
description: Two-axis review — spec Success Criteria first, then engineering standards.
---

Load the `review-code` skill and follow it. $ARGUMENTS is optional extra scope (files, PR, or commit).
